using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GroundRepeating : MonoBehaviour
{
    // this is very simple.
    // i want the player look like its running forward
    // but the player is actually running on the same z axis.
    // i need the ground keep moving until the game is over
    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
